# 2 lab

**Індвідуальне завдання**: Трикутний сигнал

**Комментарі до коду**
```python
# 2 Визначення параметрів вимірювання
t_meas = 2  # s
t_start = 0  # s
dt = 0.001

# Ми визначаємо тривалість вимірювання t_meas у секундах, початковий час t_start # у секундах та крок дискретизації dt у секундах. Це означає, що ми будемо # вимірювати значення сигналу протягом 2 секунд з кроком 0.001 секунди.

# 3 2Обчислення кількості точок вимірювання
n = (t_meas - t_start) / dt

# 4 Генерація часових відліків:
t = np.arange(t_start, t_meas, dt)

# Використовуючи numpy.arange, ми створюємо масив часових відліків t від t_start # до t_meas з кроком dt.

# 5 Визначення параметрів трикутного сигналу:
amp = 1
f = 1.0  # Hz
phi = 0.0
# Встановлюємо амплітуду сигналу amp, частоту f у Герцах та початкову фазу phi.

# 6 Генерація трикутного сигналу:
y = amp * 2 * np.abs(2 * (t * f - np.floor(t * f + 0.5))) - 1  # Triangular wave formula
# Використовуючи формулу трикутного сигналу, обчислюємо значення y для кожного відліку часу t.

# Створення графіку:
plt.figure(figsize=(10, 6))
plt.plot(t, y)
plt.xlabel('t, s')
plt.ylabel('y(t)')
plt.title('Triangle Wave Signal')
plt.grid(True)
# Використовуючи matplotlib.pyplot, створюємо фігуру розміром 10x6 дюймів. Додаємо графік y проти t, додаємо мітки для осей xlabel і ylabel, заголовок графіку title та сітку grid.

# Збереження графіку у файл:
plt.savefig('output.png')
plt.close()


```
# 4 lab

**Індвідуальне завдання**: пилоподібний сигнал

**Комментарі до коду**
```python
# 2 Визначення параметрів вимірювання
t_meas = 2  # s
t_start = 0  # s
dt = 0.001


# 3 2Обчислення кількості точок вимірювання
n = (t_meas - t_start) / dt

# 4 Генерація часових відліків:
t = np.arange(t_start, t_meas, dt)
# Створюємо масив часових відліків.

# 5 Визначення параметрів трикутного сигналу:
amp = 1
f = 1.0  # Hz
phi = 0.0

# 6 Генерація пилоподібного сигналу:
y = amp * (t * f - np.floor(t * f))

# 7 Додавання шуму до сигналу:
noise_amplitude = 0.2
noise = noise_amplitude * np.random.randn(len(t))
y_noisy = y + noise
```